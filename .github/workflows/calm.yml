name: CALM

on:
  push:
    tags:
      - "*.*.*"
      - "test-calm-*"

jobs:

  Linux:
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          sudo apt install -y build-essential
          gcc calm.c -o calm
          docker run -v $PWD:/calm -w /calm fedora:34 bash build/build.sh
          ls -lah .
      - name: GH Release
        uses: softprops/action-gh-release@v0.1.14
        with:
          prerelease: true
          files: |
            *.tgz

  macOS:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-11, macos-12, macos-10.15]

    env:
      CI_MATRIX_OS: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v3

      - name: Build
        run: |
          bash build/build.sh
          ls -lah .
      - name: GH Release
        uses: softprops/action-gh-release@v0.1.14
        with:
          prerelease: true
          files: |
            *.dmg

  Windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - uses: ilammy/msvc-dev-cmd@v1

      - name: Build
        shell: cmd
        run: |
          build\\build.bat

      - name: GH Release
        uses: softprops/action-gh-release@v0.1.14
        with:
          prerelease: true
          files: |
            *.zip
